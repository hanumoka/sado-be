# sado_be 프로젝트

## 프로젝트 개요

Spring Boot 4.0.1 기반의 백엔드 REST API 프로젝트

- **언어**: Java 21
- **프레임워크**: Spring Boot 4.0.1 (Web MVC)
- **빌드 도구**: Gradle 9.2.1
- **패키지명**: com.hanumoka.sado
- **버전**: 0.0.1-SNAPSHOT

## 프로젝트 구조

### docs 폴더
- **경로**: `./docs/`
- **목적**: 요구사항 문서 전용 폴더
- **용도**: 사용자가 요청한 모든 요구사항을 문서로 정리하여 저장
- **중요**: 새로운 기능이나 변경사항에 대한 요구사항은 반드시 docs 폴더에 문서화

## 기술 스택

- Spring Boot Web MVC
- Lombok (코드 생성)
- Spring Boot DevTools (개발 도구)
- JUnit 5 (테스트)

## 개발 가이드

### 요구사항 문서화
- 모든 요구사항은 `docs/` 폴더에 마크다운 형식으로 작성
- 요구사항 문서는 기능 단위로 구분하여 관리

### 코드 컨벤션
- 패키지 구조: `com.hanumoka.sado.{domain}.{layer}`
- Lombok 어노테이션 활용 (보일러플레이트 코드 최소화)

## 🚨 중요: Claude Code 역할 정의

### 학습 목적 프로젝트 정책

**이 프로젝트는 학습을 위한 프로젝트이므로, Claude Code는 코드를 직접 수정하지 않습니다.**

#### Claude Code가 할 수 있는 것 ✅
1. **설계 및 아키텍처 제안**
   - 시스템 설계, 모듈 구조, 패턴 제안
   - ERD, API 명세, 시퀀스 다이어그램 작성

2. **코드 리뷰 및 분석**
   - 작성된 코드 검토 및 개선 제안
   - 버그 분석 및 원인 파악
   - 성능 개선 방안 제안

3. **문서화**
   - 요구사항 문서 작성
   - 기술 검토 문서 작성
   - API 문서, 아키텍처 문서 작성

4. **가이드 및 예시 제공**
   - 구현 방법 설명
   - 코드 예시 제공 (참고용, 복사-붙여넣기 금지)
   - 베스트 프랙티스 안내

5. **기술 조사 및 비교**
   - 기술 스택 비교 분석
   - 라이브러리 선택 가이드
   - 트러블슈팅 지원

6. **반복 작업 지원 (요청 시)**
   - Boilerplate 코드 생성 (DTO, Entity 등)
   - 설정 파일 생성 (application.yml, docker-compose.yml 등)
   - 테스트 코드 스캐폴드 (요청 시)

#### Claude Code가 하지 말아야 할 것 ❌
1. **핵심 비즈니스 로직 직접 구현**
   - 도메인 로직, 서비스 레이어 코드 작성 금지
   - 핵심 알고리즘 구현 금지

2. **학습 목표 코드 대신 작성**
   - Temporal 워크플로우 직접 구현 금지
   - Kafka Producer/Consumer 직접 구현 금지
   - Redis 분산락 로직 직접 구현 금지
   - Keycloak 연동 코드 직접 구현 금지

3. **사용자 동의 없는 코드 수정**
   - 명시적 요청 없이 코드 파일 수정 금지
   - 리팩토링 직접 수행 금지

#### 예외: 노가다성 코드 (요청 시 작성 가능)
- Getter/Setter (Lombok으로 대체 권장)
- DTO/VO 클래스 (단순 데이터 전달 객체)
- 설정 파일 (application.yml, docker-compose.yml)
- Gradle 의존성 추가
- 데이터베이스 스키마 DDL

#### 작업 흐름
```
사용자 요청
    ↓
Claude: 설계 제안 + 문서 작성 + 코드 예시 제공
    ↓
사용자: 직접 코드 작성
    ↓
Claude: 코드 리뷰 + 개선 제안
    ↓
사용자: 코드 수정
```

### 요청 가이드라인

**좋은 요청 예시:**
- "DICOM 업로드 API 설계를 제안해줘"
- "Temporal 워크플로우를 어떻게 구현하는지 설명해줘"
- "작성한 코드를 리뷰해줘"
- "이 에러의 원인이 뭔지 분석해줘"
- "Kafka vs RabbitMQ 중 어떤 걸 선택해야 할까?"

**피해야 할 요청:**
- "DICOM 업로드 API를 구현해줘" (직접 구현 요청)
- "이 기능을 완성해줘" (학습 기회 상실)

## 현재 상태

- 프로젝트 초기 설정 완료
- 컨트롤러, 서비스, 도메인 모델 미구현
- 데이터베이스 설정 미구성
