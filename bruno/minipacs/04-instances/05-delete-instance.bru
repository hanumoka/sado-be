meta {
  name: Delete Instance
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/instances/{{instanceId}}
  body: none
  auth: none
}

docs {
  # Delete Instance

  Delete an instance record.

  ## Path Parameters
  - `id`: Instance ID
}
